osx_image: xcode13.4
os: osx
language: node_js
node_js: '16'
before_install:
  - npm i -g npm@8.8.0
env:
  - ELECTRON_CACHE=$HOME/.electron
script:
  - npm run validate && npm run dist:macos-and-linux && npm run upload-sourcemaps
branches:
  only:
    - master
    - prod
    - build
